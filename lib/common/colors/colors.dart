import 'package:flutter/material.dart';

class AppColors {

  static const Color whiteColor = Colors.white;

  /// 主背景
  static const Color primaryBackground = Color(0xFFF3F3F3);
  
  /// 主色调 黄
  static const Color primaryColor = Color(0xFFFFC83E);
  
  /// 主色调 褐色
  static const Color primaryColorA56D5F = Color(0xFFA56D5F);

  /// 辅助色 绿
  static const Color assistColorGreen = Color(0xFF2CA690);

  /// 辅助色 橙
  static const Color assistColorOrange = Color(0xFFF49627);

  /// 辅助色 橙
  static const Color assistColorBlue = Color(0xFF4480DA);

  /// 辅助色 橙
  static const Color assistColorRed = Color(0xFFEC655A);

  /// 黑色字体
  static const Color textColor = Colors.black;

  /// 主文本灰色
  static final Color primaryTextColor = textColor.withOpacity(0.9);

  /// 主文本灰色
  static final Color primarySecondTextColor = textColor.withOpacity(0.8);

  /// 主文本灰色
  static final Color assistTextColor = textColor.withOpacity(0.6);

  /// 主文本灰色
  static final Color promptTextColor = textColor.withOpacity(0.4);

  /// 主文本灰色
  static final Color divideLineColor = textColor.withOpacity(0.05);
}
